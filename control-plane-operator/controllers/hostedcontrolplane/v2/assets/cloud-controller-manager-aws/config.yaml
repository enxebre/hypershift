apiVersion: v1
data:
  aws.conf: |-
    [Global]
    Zone = %s
    VPC = %s
    KubernetesClusterID = %s
    SubnetID = %s

    [LoadBalancer]
    ClusterServiceLoadBalancerHealthProbeMode = "Shared"
kind: ConfigMap
metadata:
  name: aws-cloud-config
