apiVersion: v1
data:
  aws.conf: |-
    [Global]
    Zone = %s
    VPC = %s
    KubernetesClusterID = %s
    SubnetID = %s

    [Shared]
    DisableSecurityGroupIngress = true
kind: ConfigMap
metadata:
  name: aws-cloud-config
